apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: syslog-pv-claim
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: csi-sc-cinderplugin
